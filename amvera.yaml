meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  additionalCommands: npx prisma generate
  artifacts:
    "*": /
    "src": /
    "public": /
    "prisma": /

run:
  command: npx prisma db push && npm run start
  containerPort: 3000
